/.phpunit.cache
.phpunit.result.cache
/public/hot
/public/storage
/storage/*.key
.env
.env.backup
.env.production
.env.local
Homestead.json
Homestead.yaml
auth.json
npm-debug.log
yarn-error.log
/.fleet
/.idea
/.vscode

# Local sample / source documents (not app code)
/Current Rent File Sample/

# vendor/, node_modules/, and public/build are tracked intentionally
# so the app can be deployed to cPanel without composer/npm CLI access.
