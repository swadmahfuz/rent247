<IfModule mod_rewrite.c>
    Options -Indexes +FollowSymLinks
    RewriteEngine On

    # Leave requests that already target /public alone
    RewriteRule ^public/ - [L]

    # Map the app root and all paths into public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
